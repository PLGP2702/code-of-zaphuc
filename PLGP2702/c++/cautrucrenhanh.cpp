#include <iostream>
#include <math.h>

using namespace std;

//abs(x) : tri tuyet doi 
//pow(a,b) : tinh a ^ b : tra ve so double
//sqrt(n) : tin can bac hai cua n : tra ve so double 
// round(a) : lam tron so
//100 > 50 : tue =>
int main(){
    /*
    int n = 200;
    if((n >= 100) && (n <= 500)){
        cout << "so chan! \n";
    }
    */
    
    return 0;  
}

